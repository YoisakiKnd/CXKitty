# CxKitty Web

CxKitty Web is a NiceGUI-based web application for automating Chaoxing course tasks.

## Run Locally

```bash
uv sync --dev
uv run python app.py
```

The application starts a local web server and opens the browser automatically on Windows.

## Package for Windows

This repository includes a GitHub Actions workflow that builds a Windows package with PyInstaller.
